# Initialize Git repository
git init

# Configure Git (if not already configured)
git config user.email "codeagent@valhalla.dev"
git config user.name "CodeAgent"

# Add all files
git add .

# Create initial commit
git commit -m "Initial commit: Simple Hello World app"